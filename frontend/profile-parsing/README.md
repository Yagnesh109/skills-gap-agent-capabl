# Profile Parsing Frontend

This is a simple file upload page for the resume parsing flow.

## Run locally

From the project root:

```bash
cd frontend
npm install
npm run dev -- --host 0.0.0.0
```

Then open the local Vite URL shown in the terminal.
